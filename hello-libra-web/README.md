# Simple scheme web project

Library libra is used (with little fix). Started with libra demo project.

## Build & run docker

```
docker build . -t scheme_web
docker run -p 8011:8011 --rm scheme_web
```
